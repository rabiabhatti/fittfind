import React from 'react';

import '../styles/right_bar.css'
import scrollDownIcon from '../images/scroll_down_icon.png'

const RightBar = (props) => {
    const { totalSliders, currentSliderNo } = props
    return (
        <div className='section-Right-bar'>
            {/*{if }*/}
        </div>
    )
};

export { RightBar }
